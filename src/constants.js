export const CARD_STATUS = {
  DONE: 'done',
  TODO: 'todo',
  DEFAULT: 'feeds',
  IN_PROGRESS: 'inprogress'
};

// update chaque n heures (secondes, (minutes, heure), millisecondes)
export const UPDATE_INTERVAL_MS = (60 * (60 * 0.5) * 1000);
